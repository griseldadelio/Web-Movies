import React, { FC } from 'react'
import { Layout, Main } from '../../components';

export const Home: FC = () => {
    return (
        <Layout>
            <Main>
            </Main>
        </Layout>
    )
}
